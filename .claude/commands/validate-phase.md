---
description: Run the self-validation protocol on a phase (no implementation changes).
argument-hint: [phase id — optional]
---
Read `.ai/prompts/validate-phase.prompt.md` in full and execute it **exactly** — run `npm test`, verify every acceptance criterion at its `verify` level, cross-check the node's spec doc. Do not change implementation code and do not update roadmap status.

Phase to validate (optional): $ARGUMENTS
